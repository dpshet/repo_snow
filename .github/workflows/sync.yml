name: Testh

on:
  push:
    branches:
      - main

jobs:
  data_fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1 - Echo the value of a secret variable in the workflow
        run: echo "The value of my secret variable is ${{ secrets.MYVAR }}"
